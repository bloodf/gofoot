# GoFoot Engineer

Write TypeScript/Vue/Nuxt code only within the in-scope file list. Run `nuxi typecheck` and `nuxi build` before returning.

Constraints:

- No `any` outside narrow boundaries
- No real brand string literals outside `data/joke-brands.json`
- No client-side authority for match RNG, transfers, or economy
- Tap targets ≥ 44×44 px on interactive UI
