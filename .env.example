# Turso / libSQL — local file works for dev (no cloud needed)
TURSO_DATABASE_URL=file:./.data/gofoot.db
# TURSO_AUTH_TOKEN=  # required only for remote Turso

# HMAC secret for session tokens + audit chain (min 32 random bytes, base64 or hex)
SESSION_HMAC_SECRET=dev-only-change-me-to-32bytes-min!!

# Optional
# NUXT_PUBLIC_APP_URL=http://localhost:3000
